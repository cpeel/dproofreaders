# Local DevEx - work in progress / hack

```bash
# from repo base
npm install
npx parcel SETUP/devex/page_browser.html
```

Open the local URL.

Edit `page_browser.html` to set your `userApiKey`.

Develop away!

TODO:
* Figure out a way for devs to set their API key locally without changing
  anything in the repo.
* Figure out building / packaging JS & CSS.
